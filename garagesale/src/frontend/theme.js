// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#556cd6',
    },
    secondary: {
      main: '#19857b',
    },
    // You can add more customization here
  },
  // Add other theme customizations such as typography, breakpoints, etc.
});

export default theme;
